import styled from "styled-components";

export const FormLabelSty = styled.label`
    color: ${prop => prop.theme.text};
    display: flex;
    align-items:center;
    justify-content: center;
    height: 100%;
    font-weight: bold;
`