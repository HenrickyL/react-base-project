import styled from "styled-components";

export const NavigateRootSty = styled.nav`
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-around;
    gap: 8px;

    a{
        text-decoration: none;
        color: inherit;
    }  
`