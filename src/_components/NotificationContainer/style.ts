import styled from "styled-components";

export const NotificationContainerSty = styled.div`
    display: flex;
    right: 0;
    bottom: 4px;
    flex-direction: column;
    align-items: center;
    justify-content: flex-end;
    position: absolute;
    min-width: 16rem;
    gap: 4px;
    padding: 8px;
    z-index: 1;
`